# 局部源码模式测试

这是一段普通文本。点击普通文本时，不应显示任何 Markdown 标记。

## P0 行内格式

点击下面各段格式文本，检查其左右是否显示对应的 Markdown 标记：

- 这是 **加粗文本**，应显示两个星号。
- 这是 *斜体文本*，应显示一个星号。
- 这是 ~~删除线文本~~，应显示两个波浪号。
- 这是 `const answer = 42` 行内代码，应显示反引号。
- 这是 [普通链接](https://example.com)，应显示链接目标。
- 这是 [带标题的链接](https://example.com/docs "示例文档")，应同时显示 URL 和 title。
- 这是 [包含空格的链接](<docs/test file.md> "本地文档")，目标应保留尖括号。

## 嵌套格式

点击不同位置，检查只展开光标所在的完整格式范围：

- **加粗中包含 *斜体文本* 的示例**
- **加粗中包含 ~~删除线文本~~ 的示例**
- [**加粗链接文本**](https://example.com/bold "加粗链接")
- ***同时加粗和斜体***

## 行内代码边界

- 普通代码：`npm run build`
- 包含一个反引号：``a`b``
- 首尾包含反引号：`` `code` ``
- 包含空格：` hello world `

## Wiki 链接

Wiki 链接当前保持原始文本显示，不进行额外局部展开：[[测试页面]]、[[测试页面|显示别名]]。

## 图片

点击下面的图片，图片旁应显示完整 Markdown 源码：

![编辑器图标](media/icon.svg "MD Wiki Editor 图标")

带宽度属性的图片：

![限宽编辑器图标](media/icon.svg "320 像素宽度"){width=320}

## 不在本次范围

下面的标题、引用、列表和代码块不应因为光标进入而显示局部源码标记。

> 这是一段引用。

1. 有序列表项目
2. 另一个列表项目

```ts
const message = '代码块不在本次实现范围';
console.log(message);
```
