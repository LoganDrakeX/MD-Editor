# VS Code Markdown Wiki 编辑器插件 — 技术方案

> 目标：一个嵌入 VS Code 的 Markdown 编辑/预览插件，Wiki 风格，支持 **WYSIWYG / 源码 / 预览** 三种模式；快捷键一键打开并加载当前 md 文件；顶部为编辑工具栏；支持粘贴图片并自动存入 md 同目录下的 `images/` 子目录（目录名可在设置中修改）。

---

## 1. 需求拆解

| 需求 | 说明 |
|---|---|
| 嵌入 VS Code | 用 Webview 承载 UI，作为扩展宿主之外的"前端" |
| 三模式 | WYSIWYG（所见即所得）/ 源码（纯 Markdown 文本）/ 预览（渲染后的只读页面） |
| 快捷键打开 | 一个命令 + 默认快捷键，读取当前活动编辑器的 md 文件内容并载入面板 |
| 上方有编辑器 | 面板顶部为编辑工具栏 + 模式切换条；下方为当前模式的内容区（"上编辑下预览"的分屏作为可选布局） |
| 粘贴图片 | 在编辑器内 Ctrl+V 图片（或拖入），自动保存到 md 同目录 `images/` 子目录，并插入相对路径引用 |
| 可配置 | 图片目录名、模式、自动保存、快捷键等均通过 VS Code 设置（settings.json）控制 |
| Wiki 风格 | （可选阶段）`[[双链]]` 渲染与跳转、大纲 |

---

## 2. 总体架构

```
┌───────────────────────── VS Code ─────────────────────────┐
│                                                           │
│  扩展宿主 (Node/TypeScript)          Webview (前端)         │
│  ┌──────────────────────┐   postMessage   ┌─────────────┐ │
│  │ extension.ts         │ ◄──────────────► │ 工具栏/模式条 │ │
│  │  · 命令/快捷键注册     │    (JSON 消息)    │ WYSIWYG区    │ │
│  │  · WebviewPanel 管理  │                 │ 源码区       │ │
│  │  · 文件读写 (fs)      │                 │ 预览区       │ │
│  │  · 图片保存 handler   │                 └─────────────┘ │
│  │  · 配置读取/变更订阅   │                                  │
│  └──────────────────────┘                                  │
│         ▲  workspace.fs / TextDocument                      │
│         ▼                                                   │
│  ┌─────────────────────────────────────────────┐           │
│  │  磁盘上的 *.md + images/ 图片文件            │           │
│  └─────────────────────────────────────────────┘           │
└───────────────────────────────────────────────────────────┘
```

**核心原则：单一数据源。** 三个模式共享同一份 Markdown 字符串（`store`）：
- WYSIWYG 模式把 Markdown 解析进编辑器，任何编辑都导出回 `store`；
- 源码模式直接编辑 `store`；
- 预览模式把 `store` 渲染成 HTML（只读）；
- 切换模式时用 `store` 重建对应视图；保存时统一把 `store` 写回文件。

---

## 3. 技术选型

| 模块 | 选型 | 理由 |
|---|---|---|
| WYSIWYG 引擎 | **Milkdown v7**（`@milkdown/kit`） | Markdown 原生 WYSIWYG，文档即 Markdown，天然适合三模式切换；官方提供 Crepe 开箱编辑器与 wiki 双链支持；插件体系（slash、tooltip、table、image 等）齐全。对照方案：TipTap（生态更成熟但非 Markdown 原生，需自建 md↔ProseMirror 转换，源码模式要额外做） |
| 源码模式 | **CodeMirror 6** + `@codemirror/lang-markdown` | 专业的代码编辑体验（缩进、括号、选区），比裸 textarea 好 |
| 预览渲染 | **markdown-it** + highlight.js（可选 mermaid / KaTeX） | 成熟、可定制；GFM 表格、代码高亮开箱即用 |
| 前端框架 | **React**（可选：纯 TS + DOM） | Milkdown 官方提供 React 绑定；组件化管理工具栏/模式条 |
| 构建 | **esbuild**（扩展 + webview 打包）+ `@vscode/vsce` 打包 | 快、配置少；VS Code 官方推荐 |
| 文件/图片 IO | VS Code `workspace.fs` / `fs` | 统一 URI 处理，跨平台（Windows 路径）安全 |
| 参考项目 | [zaaack/vscode-markdown-editor](https://deepwiki.com/zaaack/vscode-markdown-editor/2.1-extension-host)、[Milkdown 官方文档](https://milkdown.dev/docs/guide/getting-started) | 完整可借鉴的 Milkdown + VS Code 扩展示例 |

---

## 4. 扩展宿主设计（`src/extension.ts` 等）

### 4.1 命令与快捷键
- 命令：`md-editor.open` —— 读取 `vscode.window.activeTextEditor` 当前文件，若为 Markdown 则创建/复用 `WebviewPanel` 并载入内容。
- 默认快捷键：`Ctrl+Alt+M`（`when: editorLangId == markdown`），用户可在 keybindings 里覆盖。*备选：`Ctrl+K M`。*
- 附加命令（内部）：模式切换、图片保存（由 webview 消息触发）。

### 4.2 WebviewPanel 生命周期
- `retainContextWhenHidden: true` —— 切走再切回不丢状态；
- `registerWebviewPanelSerializer` —— 窗口重载后恢复面板；
- `onDidDispose` 清理监听器与定时器；
- 单文件单面板：一个面板只服务一个 md 文件，打开其他文件时复用/新建。

### 4.3 文件同步
- webview 内容变更 → `content-changed` 消息 → **防抖（默认 800ms，可配置）** 写回文件（`workspace.fs.writeFile`，只写变化）；
- 外部修改文件（如 git checkout、其他编辑器）→ 通过 `workspace.onDidChangeTextDocument` 感知 → 若与本地编辑无冲突则自动刷新 webview（可配置关闭）；
- 当前实现使用 **CustomTextEditorProvider**（自定义文本编辑器 API），由 VS Code 原生管理 tab 脏状态、标题、保存、关闭确认与 Hot Exit；快捷键通过 `vscode.openWith` 打开该编辑器。

### 4.4 设置读取
- 启动时读取配置并通过 `load` 消息下发；订阅 `workspace.onDidChangeConfiguration`，变更时推送 `settings-changed` 实时生效。

---

## 5. Webview 前端设计

### 5.1 布局（"上方有编辑器"）
```
┌─────────────────────────────────────────────────┐
│ [WYSIWYG | 源码 | 预览]   B I H1 H2 | 表格 图片 │  ← 顶部模式条 + 编辑工具栏
├─────────────────────────────────────────────────┤
│                                                 │
│          当前模式的内容区（编辑器/预览）          │
│                                                 │
└─────────────────────────────────────────────────┘
```
- 模式条在最上方，点按切换三模式；
- 工具栏按模式显示（源码模式下显示格式化按钮，插入文本到光标；预览模式只读，工具栏隐藏/禁用）；
- **可选分屏开关** `mdEditor.splitView`：上编辑器 + 下预览同屏（类似 Typora 分屏），默认关闭。

### 5.2 三模式实现
| 模式 | 实现 | 同步方式 |
|---|---|---|
| WYSIWYG | Milkdown 实例，启用 commonmark / gfm / history / list / table / image / tooltip / slash / clipboard 等 kit 插件 | `listener` 插件监听变更 → 导出 Markdown → 更新 `store` |
| 源码 | CodeMirror 6 + markdown 语言 | 输入变更 → 更新 `store` |
| 预览 | markdown-it（GFM + 高亮 + 可选 mermaid/KaTeX） | 由 `store` 渲染，只读 |

模式切换：进入某模式时用当前 `store` 重建视图（WYSIWYG：`EditorView` 重新解析；源码：设置文档内容；预览：重新渲染）。**切换前先强制导出一次**，避免丢失未同步编辑。

### 5.3 通信协议（postMessage 类型化消息）
- **webview → 宿主**：
  - `{ type: 'ready' }`
  - `{ type: 'content-changed', content }`
  - `{ type: 'save-image', data(base64), mime, originalName }`
  - `{ type: 'insert-image', relativePath }`（兜底：宿主算好路径后由 webview 插入）
- **宿主 → webview**：
  - `{ type: 'load', content, filePath, settings }`
  - `{ type: 'image-saved', relativePath, absolutePath }`
  - `{ type: 'settings-changed', settings }`
  - `{ type: 'external-change', content }`

### 5.4 安全
- 严格 CSP：仅允许自身脚本与样式，`nonce` 注入，禁用远程资源；
- `localResourceRoots` 仅开放工作区 + `media`，图片通过 `webview.asWebviewUri` 引用（本地磁盘图片在 WYSIWYG/预览中可显示）。

---

## 6. 图片粘贴流程（核心需求）

```
剪贴板/拖放图片
   │  webview 内监听 paste / drop
   ▼
从 clipboardData.items 中取 image/* → File → ArrayBuffer → base64
   │  发 { type:'save-image', data, mime, originalName }
   ▼
扩展宿主 imageHandler
   │  ① 取当前 md 文件的目录 dir
   │  ② imagesDir = join(dir, settings.mdEditor.imageFolder)   // 默认 "images"
   │  ③ 递归创建目录（mkdir recursive）
   │  ④ 文件名 = 时间戳 + 原文件名（策略可配置），写文件
   │     workspace.fs.writeFile(URI.file(...))
   ▼
计算相对路径：从 md 所在目录到图片的路径（统一正斜杠 /）
   │  回发 { type:'image-saved', relativePath }
   ▼
webview 在光标处插入 Markdown 图片引用
   ├── WYSIWYG：调用 image 命令插入图片块 → 立即渲染
   └── 源码：replaceRange 插入 ![name](relativePath)
```

**细节与边界**
- 粘贴的是图片文件本身（如截图、Ctrl+C 图片）→ 走上面流程；
- 剪贴板是**文件路径**（如资源管理器里复制图片文件）→ 直接从路径保存副本，避免重复；
- 粘贴纯文本/URL → 保留原文，不干预；
- 同一 md 文件重复粘贴同名图片 → 时间戳前缀保证不覆盖（策略可配置）；
- 失败处理：目录不可写 / 磁盘满 → 弹窗提示，不破坏编辑器内容。

---

## 7. 配置项（`contributes.configuration`）

```jsonc
// 设置前缀: mdEditor
{
  "mdEditor.defaultMode": "wysiwyg",        // 打开面板时的默认模式: wysiwyg | source | preview
  "mdEditor.imageFolder": "images",          // 图片子目录名（相对 md 文件所在目录）
  "mdEditor.imageNameMode": "timestamp",     // 文件名策略: original | timestamp | timestamp-original
  "mdEditor.autoSaveDelay": 800,             // 内容写回防抖毫秒数
  "mdEditor.syncFromDisk": true,             // 外部改动文件时是否自动刷新
  "mdEditor.splitView": false,               // 可选：上编辑下预览分屏
  "mdEditor.enableWikiLinks": true           // 可选：[[双链]] 渲染
}
```

---

## 8. Wiki 风格增强（可选阶段）

- `[[页面名]]` 双链：WYSIWYG 中渲染为链接块（Milkdown 提供 wiki 支持），预览中渲染为可点击链接；
- 点击跳转：打开同目录/工作区同名 md（不存在的提示创建）；
- 悬停预览（`hoverProvider`）与大纲面板列为 roadmap。

---

## 9. 项目结构与里程碑

```
md-editor/
├── package.json            # 扩展清单: contributes.commands/keybindings/configuration
├── tsconfig.json
├── esbuild.js              # 打包 extension 与 webview 两份产物
├── src/
│   ├── extension.ts        # 入口: 激活、命令、快捷键
│   ├── panel.ts            # WebviewPanel 创建/复用/序列化
│   ├── imageHandler.ts     # 图片保存核心逻辑
│   ├── settings.ts         # 配置读取与变更订阅
│   ├── messages.ts         # postMessage 消息类型定义（前后端共享）
│   └── webview/            # 前端（打包进 webview）
│       ├── index.html      # CSP + nonce 模板
│       ├── main.tsx        # 入口
│       ├── App.tsx         # 布局: 模式条 + 工具栏 + 内容区
│       ├── store.ts        # 单一数据源 store + 模式状态机
│       ├── modes/wysiwyg.ts / source.ts / preview.ts
│       └── bridge.ts       # postMessage 封装
└── media/                  # 图标等静态资源
```

| 里程碑 | 内容 | 验收 |
|---|---|---|
| M1 骨架 | 脚手架、命令/快捷键、webview 打开并加载当前 md、保存 | Ctrl+Alt+M 打开面板，编辑后文件被更新 |
| M2 三模式 | WYSIWYG + 源码 + 预览，切换无丢失 | 三模式切换内容一致，可来回编辑 |
| M3 图片粘贴 | 粘贴/拖放存图 + 插入引用 + 全部设置项 | 粘贴截图 → images/ 下出现文件且正常显示 |
| M4 Wiki 与打磨 | 双链、错误处理、状态栏、README、`vsce package` 打包 | 可安装的 .vsix |

---

## 10. 风险与注意事项

1. **大文件性能**：Milkdown/ProseMirror 对超长文档吃力 → 提供"源码模式兜底"，超过阈值自动建议切换；
2. **快捷键冲突**：`Ctrl+Alt+M` 可能与用户已有绑定冲突 → 默认值 + keybindings 可改 + `when` 条件限定 markdown 文件；
3. **与现有插件冲突**：同类"粘贴图片"插件（如 Paste Image 类）并存时以本插件行为为准，可在文档中说明；
4. **Windows 路径**：相对路径统一 `/`，磁盘读写用 `path` 模块处理 `\`；
5. **CSP/安全**：webview 不加载远程脚本，防 XSS（粘贴 HTML 内容需消毒）；
6. **外部修改冲突**：文件被外部改动且本地有未保存编辑时，提示用户选择（覆盖/放弃/合并）；
7. **脏状态**：由 CustomTextEditorProvider 更新 `TextDocument`，VS Code 原生显示未保存状态并负责关闭前提示。

---

## 11. 已确认决策（2025 年拍板）

1. **布局**：顶部工具栏 + 模式条，三模式整页切换（不做默认分屏，`splitView` 开关保留为可选能力）。
2. **WYSIWYG 引擎**：Milkdown v7（`@milkdown/kit` + `@milkdown/react`）。
3. **前端框架**：React 18。
4. **默认快捷键**：`Ctrl+Alt+M`（mac 为 `Cmd+Alt+M`），`when: editorLangId == markdown`。
5. **Wiki 双链**：需要，纳入 M4。

> 实施状态：M1-M4 全部完成 ✅ + 多轮 UI/功能迭代（模式下拉、H1~H6、选中态高亮、列表互斥整表转换、表格网格选行列、**Obsidian 风格表格交互：行/列边界悬停小点→加号插入、行头/列头 6 点手柄→选中+设置菜单**、图片拖拽调宽 `{width=N}`、WYSIWYG 代码高亮、格式气泡、左侧大纲（点击跳转+拖拽调宽）、浅/深主题）。构建/类型检查/命名单测/jsdom 冒烟（70+ 项断言）通过。产物：`md-wiki-editor-0.1.0.vsix`。
>
> 已知行为约定（v0.1.0）：表格内**原生拖放一律禁用**——整行/整列选中后拖动无反应、单元格内文字拖动无反应、内容不得拖进表格、含表格结构的拖放内容在任意位置拒绝（防止在表格下方新建表格）；行列移动/插入/删除/对齐统一走行头/列头手柄与设置菜单（手柄拖拽移动行/列仍可用）。
>
> 表格复制粘贴约定（v0.1.0）：**Ctrl+A 在表格内只选中整张表**（不会连表格后的句子一起选中）——root/window 捕获级 keydown 拦截 + keymap 兜底 + **appendTransaction 事务级守卫**（即使 VS Code 宿主吞掉 keydown、浏览器原生全选同步进来，也会改写为整表 CellSelection）；粘贴内容含表格且目标在表格内时，用粘贴内容**整体替换当前整张表**；从表格下方的段落向上拖选进入表格会被阻止。复制/粘贴/全选行为已用 `scripts/repro-paste.cjs` 回归测试（接入 `npm test`）。
